# Crypto Insight Backend

Simple Django + PostgreSQL backend for a crypto dashboard application.

The API provides:
- A list of supported coins.
- (Planned) historical price data and basic price prediction endpoints.

## Tech Stack

- Python 3
- Django 5 + Django REST Framework
- PostgreSQL
- (Dev only) Local virtual environment (`venv`)
